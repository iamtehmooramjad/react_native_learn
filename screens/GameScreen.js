import React from 'react';

import {Text, View} from 'react-native';

const GameScreen = () => {
    return (
        <Text>
            Game Screen
        </Text>
    );
};

export default GameScreen;
